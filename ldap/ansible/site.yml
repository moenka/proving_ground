---
- hosts: all
  become: true
  roles:
    - role: willshersystems.sshd
  tasks:

    - name: install packages
      yum:
        name: "{{ item }}"
        state: present
      with_items:
        - curl
        - git
        - htop
        - openldap-servers
        - vim

    - name: remove packages
      yum:
        name: "{{ item }}"
        state: absent
      with_items:
        - firewalld
        - firewalld-filesystem

